import { StyleSheet } from "react-native";

export const colors = StyleSheet.create({
    firstColor: {
        light: {
            color: "#000",
            backgroundColor: "#eee"
        },
        dark: {
            color: "#fff",
            backgroundColor: "#333"
        }
    },
    secondColor: {
        light: {
            color: "#333",
            backgroundColor: "#ddd"
        },
        dark: {
            color: "#fff",
            backgroundColor: "#555"
        }
    }
});